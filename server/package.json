{
  "name": "fuerteventura-news-proxy",
  "version": "0.1.0",
  "description": "Tiny Express proxy to fetch RSS feeds and return JSON (for local dev)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "feedparser": "^2.2.10",
    "node-fetch": "^2.6.7",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "jest": "^25.0.0",
    "supertest": "^7.0.0"
  }
}
