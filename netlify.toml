[build]
  publish = "."
  functions = "netlify/functions"
  # Nota: node_bundler no es válido en [build]; se configura en [functions]

[functions]
  node_bundler = "esbuild"
  external_node_modules = []
  included_files = []

[build.environment]
  NODE_VERSION = "18"
